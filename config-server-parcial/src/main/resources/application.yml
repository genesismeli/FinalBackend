server:
  port: ${PORT:8888}

spring:
  application:
    name: config-server-parcial
  cloud:
    config:
      server:
        git:
          uri: https://github.com/genesismeli/Configuration-final.git
          default-label: main
          force-pull: true
#          search-paths: configuration-parcial
